import Init

namespace Large

-- Selected task statements. Accepted candidates must supply independent proofs.
theorem multiplication (a b c : Nat) : (a * b) * c = a * (b * c) := by sorry

theorem reversal (xs : List Nat) : xs.reverse.reverse = xs := by sorry

end Large
