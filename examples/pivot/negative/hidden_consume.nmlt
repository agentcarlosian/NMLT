system Abstract {
  state bit: Bool = false
  observe bit
}

system Concrete {
  state bit: Bool = false
  capability token: Once<Unit>
  action tick {
    consume token
  }
  observe bit
}

refine Concrete refines Abstract {
  map state bit -> bit
  hide action tick
}
