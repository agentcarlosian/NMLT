import Mathlib.Data.Nat.Choose.Basic

namespace App

/-- There is exactly one way to choose no elements from a finite collection. -/
theorem choose_nothing : ∀ n : Nat, Nat.choose n 0 = 1 := by
  sorry

end App
