{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nmlt.dev/schemas/model-check-report.schema.json",
  "title": "NMLT deterministic model-check report",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "system",
    "result",
    "complete",
    "explored_states",
    "explored_transitions",
    "bounds",
    "properties"
  ],
  "properties": {
    "schema_version": { "const": "1.0.0" },
    "system": { "type": "string", "minLength": 1 },
    "result": { "$ref": "#/$defs/result" },
    "complete": { "type": "boolean" },
    "explored_states": { "type": "integer", "minimum": 1 },
    "explored_transitions": { "type": "integer", "minimum": 0 },
    "bounds": {
      "type": "object",
      "additionalProperties": false,
      "required": ["max_states", "max_depth"],
      "properties": {
        "max_states": { "type": "integer", "minimum": 1 },
        "max_depth": { "type": "integer", "minimum": 0 }
      }
    },
    "properties": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["property", "result", "reason", "witness"],
        "properties": {
          "property": { "type": "string", "minLength": 1 },
          "result": { "$ref": "#/$defs/result" },
          "reason": { "type": "string", "minLength": 1 },
          "witness": {
            "type": ["object", "null"],
            "additionalProperties": false,
            "required": ["steps"],
            "properties": {
              "steps": {
                "type": "array",
                "minItems": 1,
                "items": { "$ref": "#/$defs/step" }
              }
            }
          }
        }
      }
    }
  },
  "allOf": [
    {
      "if": {
        "properties": { "result": { "const": "model_checked" } },
        "required": ["result"]
      },
      "then": {
        "properties": { "complete": { "const": true } }
      }
    }
  ],
  "$defs": {
    "result": {
      "enum": ["model_checked", "refuted", "unknown"]
    },
    "value": {
      "type": ["boolean", "integer", "string"]
    },
    "step": {
      "type": "object",
      "additionalProperties": false,
      "required": ["index", "action", "state", "available_capabilities", "enabled_actions"],
      "properties": {
        "index": { "type": "integer", "minimum": 0 },
        "action": { "type": ["string", "null"] },
        "state": {
          "type": "object",
          "additionalProperties": { "$ref": "#/$defs/value" }
        },
        "available_capabilities": {
          "type": "array",
          "items": { "type": "string" },
          "uniqueItems": true
        },
        "enabled_actions": {
          "type": "array",
          "items": { "type": "string" },
          "uniqueItems": true
        }
      }
    }
  }
}
