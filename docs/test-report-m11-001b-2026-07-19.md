# M11-001b local verification report — 2026-07-19

## Scope

This report covers the repository state after implementing M11-001b and
installing every tool used by the active local test, comparison, evidence, and
metatheory gates. It tests the working tree; it is not a clean-commit or remote
CI attestation.

## Environment preflight

All required versions and identities were resolved locally:

| Component | Verified version or identity |
|---|---|
| Rust | `rustc 1.94.0 (4a4ef493e 2026-03-02)` |
| Python | `3.12.3` |
| Node / npm | `25.8.1` / `11.11.1` |
| Quint | `0.32.0` |
| Java | OpenJDK `21.0.11` |
| .NET SDK | `8.0.423` |
| P | `3.1.0.0` |
| Elan | `4.2.3` |
| Lean / Lake | Lean `4.30.0`; Lake `5.0.0-src+d024af0` |
| TLC | `2.19`, revision `5a47802` |
| TLC jar | SHA-256 `936a262061c914694dfd669a543be24573c45d5aa0ff20a8b96b23d01e050e88` |
| lean4export | commit `a3e35a584f59b390667db7269cd37fca8575e4bf` |
| NanoDA | commit `f58f2f6d535e189a40fcb02ede8eb95f97a92d37` |

Noninteractive jobs use `nmlt-test-env` so `TLA2TOOLS_JAR` and `DOTNET_ROOT`
are explicit instead of relying on interactive shell startup.

## Focused M11-001b executable tests

Commands:

```sh
cargo fmt --all --check
cargo clippy -p nmlt-temporal --all-targets -- -D warnings
cargo test -p nmlt-temporal --all-targets
```

Result: **pass**, 46 tests, 0 failures. The M11-001b controls covered:

- canonical order-independent but nominal payload identities;
- finite predicate validation and exact-type inclusion;
- identity and positive nonidentity refinement;
- nonidentity witness composition;
- assumption contravariance and rejection of assumption strengthening;
- guarantee covariance and rejection of guarantee weakening;
- rejection of payload substitution with structurally equal variants;
- rejection of hidden, incomplete, and noninjective boundary mappings;
- exact connected contract discharge;
- rejection of missing and circular discharge;
- preservation of namespaced unconnected boundary contracts; and
- fail-closed product work limits and global input receptiveness.

## Evidence and schema controls

The M11-001a and M11-001b evidence checkers, benchmark-integrity self-test,
and generic evidence adversarial self-test all passed. The M11-001b checker
accepted the exact source set, schema, theorem handles, Rust control handles,
limitations, and manifest identity. Its forged-correspondence and stale-source
controls remained rejected.

## Lean kernel and axiom audit

`./tools/check_metatheory.sh` completed all 17 Lean build jobs. The following
M11-001b declarations reported no axioms:

- `PredicateSubset.refl`
- `PredicateSubset.trans`
- `Refinement.identity`
- `Refinement.compose`
- `Refinement.exactPayloadIdentity`
- `Refinement.abstractAssumptionIncluded`
- `Refinement.concreteGuaranteeIncluded`

The policy scan found no forbidden `sorry`, `sorryAx`, `admit`,
`native_decide`, or project-defined axiom in the checked sources.

## Independent NanoDA replay

The locally installed pinned exporter and NanoDA checker replayed the complete
`NMLT` environment. Result: **95,343 declarations checked with no errors**.
The configuration did not permit `sorryAx`.

## Executable comparison suite

`nmlt-test-env ./tools/validate_comparisons.sh` ran every branch with no
optional skip:

| Model | Result |
|---|---|
| NMLT | Complete finite frontier: 5 states, 6 transitions |
| Quint 0.32.0 | Parse/typecheck passed |
| TLC 2.19 | 8 states generated, 7 distinct, queue exhausted, no error |
| P 3.1.0 | Compiled successfully; 100 schedules explored; 0 bugs |

TLC's result is exhaustive for the declared finite comparison model. P's
result is a 100-schedule randomized systematic-testing run, not an exhaustive
proof.

## Integrated and archive gates

`nmlt-test-env make reproduce` passed. This includes workspace formatting,
checking, warning-denied Clippy, all Rust tests, all evidence readbacks,
comparison sources and executions, M9 correspondence/vertical-slice checks,
and the Lean metatheory gate.

The `/home/carlos/Math` archive check also passed: 2,347 indexed records, 13
additions, 8 overlays, 8 research loops, and every checkpointed file digest
matched.

## Residual limitations

- M11-001b does not prove Rust/Lean correspondence.
- Product congruence for the new relation remains M11-001c work.
- The profile is finite and safety-oriented; it excludes payload subtyping,
  representation conversion, capabilities, grades, circular fixed-point
  contracts, fairness, divergence, and liveness transport.
- The P result is sampled over 100 schedules.
- Local tool versions and content identities are checked, but bootstrap and
  package downloads are not signed or reproducibly attested by this report.
- Remote GitHub CI was not invoked; this report records local execution only.
- The tested repository contains the uncommitted M11-001b working-tree changes.

## Verdict

**Pass at the declared finite M11-001b scope.** No failing executable,
evidence, comparison, Lean-kernel, independent-checker, integrated, or archive
gate remains in the tested local environment.
